#define NUMBER_OF_TUBES 7
#define TUBE_CROSSINGS 1 // Set to 0 to disable; faster
#define ANIMATION_SPEED 20000000 // Increase for slower animation
#define USE_COLORS 1 // Use colors if available
#define DIFICULTY_OF_SPLIT 3 // How likely (1/n) it is that a certain pipe will change direction
